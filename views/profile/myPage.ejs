<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= user.name %>'s Profile</title>
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" 
        rel="stylesheet"
    >
    <link rel="stylesheet" href="/my_page.css">
</head>
<body class="bg-light">
    <%- include('../navbar') %>
    <div class="container" style="margin-top: 8em;">
        <div class="card shadow rounded-4">
            <div class="card-body">
                <h2 class="card-title mb-3 text-primary">Welcome, <%= user.name %>!</h2>
                <hr>
                <h5 class="mt-4">Manage Your Links:</h5>
                <div class="scroll-box mt-2">
                    <% links.forEach(function({ id, shortCode, url }) { %>
                        <form action="/update-link/<%= id %>" method="POST" class="mb-3">
                            <div class="input-group">
                                <span class="input-group-text fw-semibold" style="min-width: 100px;"><%= shortCode %></span>
                                <input 
                                    type="text" 
                                    name="url" 
                                    value="<%= url %>" 
                                    class="form-control url-input"
                                    required
                                >
                                <button type="submit" class="btn btn-success">Save</button>
                            </div>
                        </form>
                    <% }); %>
                </div>
            </div>
        </div>
        <a href="/" class="btn btn-outline-secondary mt-3">
            ← Back
        </a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
